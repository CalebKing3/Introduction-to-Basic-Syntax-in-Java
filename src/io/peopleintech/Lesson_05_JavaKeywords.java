package io.peopleintech;

public class Lesson_05_JavaKeywords {

    /*
    * Keywords are reserved words that have special meaning in Java.
     * */

    // ? For example,
    // ! public
    // ! private
    // ! static
    // ! class
    // ! main
    // ! new
    // ! instanceof
    // ? are keywords in Java, and as such, we can't use them as identifiers (variable names).
}
